library verilog;
use verilog.vl_types.all;
entity instrdec is
    port(
        op              : in     vl_logic_vector(6 downto 0);
        ImmSrc          : out    vl_logic_vector(1 downto 0)
    );
end instrdec;
