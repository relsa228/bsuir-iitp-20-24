Сборка: `sudo makefile`

/*********************************************************/

Запуск демона: `sudo ./desulogd`

/*********************************************************/

Терминация одного процесса: `sudo ./desulogd -q *pid*`

Терминация всех процессов: `sudo ./desulogd -q`

/*********************************************************/

Список протоколируемых процессов (изменение флага) и расположение выходного файла указываются в конфиге
